# === DB ===
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/safesnapdb}
spring.datasource.username=${DB_USERNAME:safesnap}
spring.datasource.password=${DB_PASSWORD:safesnap123}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# === JWT ===
jwt.secret=${JWT_SECRET:uqGxrUwv74rDuyxWE7c1I2VuCugiCnhV11FS3X2PxvU=}

# === AWS S3 ===
aws.access-key-id=${AWS_ACCESS_KEY_ID}
aws.secret-access-key=${AWS_SECRET_ACCESS_KEY}
aws.region=${AWS_REGION}
aws.s3.bucket-name=${AWS_S3_BUCKET_NAME}


# === Google Cloud Vision API ===
google.application.credentials=${GOOGLE_APPLICATION_CREDENTIALS:}
